from pathlib import Path

# determine root directory, this assumes that the the assg_utils.py
# file is in a child directory 1 level below the project root
PROJECT_ROOT = Path(__file__).resolve().parent.parent
